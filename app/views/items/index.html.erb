<%= render 'home/nav'%>
<p style="color: green"><%= notice %></p>

<div class="group-number mb- d-flex px-3 bg-white justify-content-between">
  <p>Amount Spent</p>
  <p>4</p>
</div>

<div class="group-number mb-5 d-flex px-3 bg-white justify-content-between">
  <p>Number of payments</p>
  <p>4</p>
</div>

<div class="pt-4 ps-3">
<h1>Items</h1>

<div id="items">
  <% @items.each do |item| %>
    <%= render item %>
    <p>
      <%= link_to "Show this item", item %>
    </p>
  <% end %>
</div>

<%= link_to "New item", new_item_path, class:"btn ms-3 mt-2 m-1 m-md-5 px-5 fixed-bottom btn-danger text-center btn-danger" %>

</div>
